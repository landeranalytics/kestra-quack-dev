# Plain value used by docker-compose to configure the quack server's auth token
DUCKDB_TOKEN=token

# Kestra secret for DuckDB token (base64 encoded) - must decode to the same value as DUCKDB_TOKEN above
# Generate with: echo -n "token" | base64
SECRET_DUCKDB_TOKEN=dG9rZW4=
